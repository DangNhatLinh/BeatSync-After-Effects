# Core DSP baseline (works today, no GPU required)
librosa>=0.10.2
numpy>=1.24,<3.0
soundfile>=0.12.1

# Pretrained neural beat tracker (M2). Optional but recommended.
# Install separately because it pulls torch:
#   pip install https://github.com/CPJKU/beat_this/archive/main.zip
# torch>=2.0  # uncomment if you want a hard dep

# Training your own model (M3)
# torch>=2.0
# torchaudio>=2.0
# tqdm>=4.66
# mir_eval>=0.7        # standard beat-tracking metrics

# Dev / notebooks
# matplotlib>=3.8
# jupyter>=1.0
